apiVersion: v1
kind: Service
metadata:
  name: clickhouse-instance
  labels:
    app: clickhouse-instance
spec:
  type: NodePort
  selector:
    app: clickhouse-instance
  ports:
    - name: native
      port: 9000
      targetPort: native
      nodePort: {{ .Values.service.nodePort }}
